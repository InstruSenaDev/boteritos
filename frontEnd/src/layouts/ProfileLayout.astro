---
interface Props {
    title?:string;
}

const {title} = Astro.props;
---


<main class="flex justify-center px-10 py-5 " >
    <article class="flex flex-col w-[90%] px-20 py-14 bg-white rounded-xl">
     <div class="w-[100%]">
        <h1 class="text-title2 font-Cocogoose-SemiLight text-darkBlue ">
            Información personal
        </h1>
         
        <div class="flex flex-row border-b-2 border-b-placeholderBlue py-7 gap-7 ">
            <slot name="imagen"/>
            <div class="flex flex-col w-full">
                <slot name="datos"/>
            </div>
        </div>

        <slot name="datos2"/>
    </article>
    </div>
</main>
