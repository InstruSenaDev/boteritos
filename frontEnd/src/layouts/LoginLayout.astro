---
import Boton from "../components/Boton.astro";
import Input from "../components/forms/Input.astro";

interface Props {
    titulo: string    
}

const {titulo} = Astro.props;


---

<main class="w-full h-screen flex justify-center items-center p-4 text-black">
    <form action="" class="bg-white md:p-20 p-7 flex gap-20 rounded-xl shadow-[0_0_20px_0px_rgba(94,175,232,0.5)]">
        <div class="flex flex-col gap-7 max-w-[400px] w-full">
            <h1 class="text-title font-cocogooseRegular tracking-widest text-darkBlue">{titulo}</h1>
            <slot name="inputs" />
            <slot name="footer" />
            
            <Boton text="Iniciar Sesión" />
        </div>
        <div class="max-h-96 max-w-[520px] w-full md:block hidden">
            <slot name="imagen" />
        </div>
    </form>
</main>